import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  title: {
    color: "red",
    fontSize: 50,
    marginTop: 50,
  },

  square: {
    height: 100,
    width: 100,
    backgroundColor: "blue"
  }
});
